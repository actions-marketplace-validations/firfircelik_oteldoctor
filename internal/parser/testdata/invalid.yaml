receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4317"
processors:

  - batch
  - memory_limiter
exporters:
  debug
service:
  pipelines:
    traces:
      receivers:
      processors:
